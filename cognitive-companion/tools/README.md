# tools/ — Layer 3 (scripts Python déterministes)

> ⛔ **HALT (B.L.A.S.T. Master System Prompt — Protocol 0)**
> Interdiction d'écrire le moindre script ici tant que :
> 1. les **questions de Discovery** ne sont pas répondues (voir `../task_plan.md`),
> 2. le **schéma de données** n'est pas confirmé (voir `../CLAUDE.md` §4),
> 3. `../task_plan.md` (Blueprint) n'est pas **approuvé**.

Scripts attendus (à partir de la phase A/T) : calcul de fluence (WPM, ratio parole/pause),
annotation métrique du transcript, génération signal famille, génération rapport ortho.
Secrets dans `../.env`. Fichiers intermédiaires dans `../.tmp/`.
